# Display Assets

`dikhololo_night_1k.hdr` is the Dikhololo Night environment from the
`pmndrs/drei-assets` collection. It is distributed by Poly Haven under CC0.
The local copy preserves the tesseract's historical reflections without a
runtime CDN dependency.
