Please group remaining modifications and perform the necessary commits.

---

Are the docs/agent-guardrails/*.md files, enough to minimise architectural drift?

---

Please add lightweight architectural drift checks for the Orac project.

Goal:
Turn the existing docs/agent-guardrails/*.md guidance into basic executable checks, without over-engineering.

Please inspect:
- AGENTS.md
- docs/agent-guardrails/*.md
- current test structure
- pyproject.toml / tooling configuration

Implement:
1. A guardrail consistency check script, preferably scripts/check_guardrails.py, that verifies:
   - docs/agent-guardrails contains no stray/untitled files
   - all markdown files follow the existing naming convention
   - all guardrail files referenced by AGENTS.md exist
   - all relative links from AGENTS.md to docs/agent-guardrails are valid
   - all relative links between guardrail documents are valid where practical

2. Add tests for the check script if the project test style supports it.

3. Add or document a simple command for running the check locally.

4. Do not introduce heavyweight dependencies unless already present.

5. Do not alter architectural rules unless necessary. This task is about validating the existing guardrail structure, not rewriting it.

After implementation, report:
- files changed
- checks added
- how to run them
- any broken references or questionable files found

---

Please implement lightweight guardrail drift checks for the Orac project.

Goal:
Add a small, standard-library-only Python checker that validates the structure and local references of docs/agent-guardrails without rewriting guardrail content or adding dependencies.

Files to inspect:
- AGENTS.md
- docs/agent-guardrails/
- current test layout
- pyproject.toml only if needed to understand pytest usage

Implement:

1. Create scripts/check_guardrails.py.

Requirements:
- Use only the Python standard library.
- Include the normal script header used in this project.
- Use argparse for CLI arguments.
- Provide a --root argument, defaulting to the project root inferred from the script location.
- Return exit code 0 when clean and 1 when issues are found.
- Print concise issue lines when broken.

The checker should validate:
- docs/agent-guardrails exists.
- Top-level markdown guardrail files follow the naming convention:
  - NN-kebab-case.md
  - examples: 00-project-principles.md, 10-architecture-boundaries.md
- Known non-markdown assets are allowed explicitly, currently:
  - table-abbreviations.csv
- Nested documentation directories, such as docs/agent-guardrails/frontend-nextjs/, are allowed and do not need to follow the top-level numeric naming convention.
- Stray/temp/editor-style files are rejected, including:
  - names containing "untitled" case-insensitively
  - files with no extension
  - backup suffixes such as ~, .bak, .tmp, .swp
  - leading dotfiles
- Local guardrail references from AGENTS.md are extracted and validated.
- Local guardrail references from guardrail markdown files are extracted and validated where practical.

Reference extraction should support:
- plain references such as docs/agent-guardrails/foo.md
- inline-code references such as `docs/agent-guardrails/foo.md`
- Markdown links such as [text](docs/agent-guardrails/foo.md)
- relative links between guardrail documents where practical

Ignore:
- http:// links
- https:// links
- anchors-only links
- email-style links
- external Markdown links

2. Add tests/test_check_guardrails.py.

Testing requirements:
- Import scripts/check_guardrails.py directly using importlib.util.spec_from_file_location.
- Do not require scripts/ to be a package.
- Unit-test reference extraction for:
  - AGENTS.md-style plain references
  - inline-code references
  - Markdown local links
  - ignored external links
- Unit-test invalid names using tmp_path, including:
  - Untitled Document
  - backup files
  - dotfiles
  - extensionless files
- Unit-test valid top-level guardrail names using tmp_path.
- Unit-test that nested guardrail docs are allowed without the top-level numeric naming convention.
- Add a repository smoke test that verifies current AGENTS.md guardrail references resolve.
- Do not make the pytest suite depend on the current repository containing docs/agent-guardrails/Untitled Document.
  - The direct checker may fail today because that file exists.
  - The tests should remain valid after that file is removed.

3. Documentation:
Add a very short note in AGENTS.md or an existing lightweight developer documentation location showing:

poetry run python scripts/check_guardrails.py
poetry run pytest tests/test_check_guardrails.py

Do not add a pyproject entry point.
Do not add dependencies.
Do not rewrite guardrail content.
Do not implement source-code architectural policy checks in this pass.

Expected current direct-check result:
- poetry run python scripts/check_guardrails.py may return non-zero while docs/agent-guardrails/Untitled Document exists.
- That is expected and should be reported clearly.

After implementation, report:
- files changed
- checks added
- how to run them
- any current issues found in the repository

---

Implement the plan.

---

Please perform a review-only architecture reconciliation pass for Orac.

Current context:
- The original architecture audit is in architecture-qa-results.md.
- Since that audit, multiple remediation passes have been completed, including:
  - protocol/schema/runtime/display alignment;
  - fail-closed protocol validation;
  - provider registry/factory;
  - explicit provider capabilities;
  - plugin service lifecycle wiring;
  - plugin execution policy and provenance;
  - PluginExecutionService extraction;
  - trusted PluginConfirmationBroker;
  - plugin timeout handling;
  - failed/timed-out plugin provenance;
  - high-level voice turn contract tests;
  - VoiceTurnController extraction;
  - Home Assistant data lifecycle documentation;
  - plugin audit persistence documentation;
  - plugin audit DB/API design documentation;
  - table abbreviation governance for plugin_invocations and plugin_audit_events.

Important:
This is a review-only pass.
Do not modify code.
Do not modify DDL.
Do not modify documentation unless explicitly asked after this review.
Do not implement runtime plugin audit persistence.
Do not implement Home Assistant population, websocket integration, state query, or device control.

Goals:
1. Cross-check architecture-qa-results.md against the current repository state.
2. For each original Top 10 issue, classify it as:
   - resolved;
   - mostly resolved;
   - partially resolved;
   - still open;
   - superseded;
   - newly risky due to later changes.
3. Re-evaluate the current architecture for coherence and boundaries of responsibility.
4. Identify any architectural regressions introduced by the remediation passes.
5. Identify any stale documentation or mismatches between docs and current code.
6. Reconfirm or revise the next recommended implementation sequence.

Please inspect:
- architecture-qa-results.md
- docs/plugin-execution-boundaries.md
- docs/plugin-audit-persistence.md
- docs/plugin-audit-db-api-design.md
- docs/home-assistant-data-lifecycle.md
- docs/agent-guardrails/table-abbreviations.csv
- src/controller/orac.py
- src/model/provider_registry.py
- src/model/plugin_execution_service.py
- src/model/plugin_confirmation_broker.py
- src/model/plugin_execution_policy.py
- src/model/plugin_router.py
- src/model/plugin_service_manager.py
- src/orac_voice/voice_turn_controller.py
- src/orac_voice/voice_loop_local.py
- plugins/home_assistant.json
- plugins/weather.json
- relevant tests under tests/

Specific checks:
1. Confirm whether provider-specific logic has genuinely moved out of Orac or whether meaningful leakage remains.
2. Confirm whether PluginExecutionService is a clean boundary or just a thin relocation with hidden coupling.
3. Confirm whether plugin policy, confirmation, timeout, and provenance form a coherent execution boundary.
4. Confirm whether denied/scaffold/unknown/confirmation-required paths still avoid plugin import.
5. Confirm whether Home Assistant remains safely scaffold-only.
6. Confirm whether HA tables are consistently treated as cache/state tables, not audit tables.
7. Confirm whether plugin audit persistence design is ready for DDL/API implementation.
8. Confirm whether table abbreviations are now consistently:
   - plugin_invocations -> plg_inv
   - plugin_audit_events -> plg_audevt
   and whether any stale plg_aud_evt references remain.
9. Confirm whether VoiceTurnController improved the boundary or merely moved the large state machine.
10. Confirm whether tests are still overfitted to private implementation details and whether that now matters.
11. Confirm whether src/controller/orac.py remains the primary pressure point and, if so, what the next safe extraction would be.
12. Confirm whether the original "Recommended Refactoring Sequence" should now be updated.

Output format:
- Executive summary.
- Updated status table for the original Top 10 issues.
- Newly discovered architectural risks, if any.
- Stale documentation/code mismatches, if any.
- Boundary assessment by subsystem:
  - core controller;
  - provider runtime;
  - plugin execution;
  - plugin service lifecycle;
  - confirmation broker;
  - voice runtime;
  - Home Assistant;
  - DB/API persistence;
  - protocol/display.
- Recommended next sequence.
- Explicit "do not do yet" list.
- Validation commands run and results.

Validation:
- poetry run python -m unittest discover -s tests

Remember:
This is a review-only pass. Do not make changes.

---

Please save results to architecture-qa-update1.md

---

Please implement the next architecture QA remediation pass for Orac: reconcile stale plugin audit documentation and add database verification for the new plugin audit objects.

Current state:
- The architecture reconciliation pass found no major new architectural drift.
- Provider, plugin policy, confirmation, timeout, provenance, service lifecycle, and voice boundary work are mostly resolved.
- The main remaining pressure point is src/controller/orac.py.
- PluginExecutionService is currently a useful seam but not yet a full execution lifecycle boundary.
- Plugin audit DB/API assets now exist.
- docs/plugin-audit-db-api-design.md and docs/home-assistant-data-lifecycle.md contain stale wording that predates the plugin audit DB/API assets.
- No runtime plugin audit persistence should be wired in this pass.

Goals:
1. Reconcile stale documentation around plugin audit DB/API status and HA next steps.
2. Add database/static verification for plugin audit objects, grants, constraints, indexes, triggers, views, and package validity, following existing project conventions.
3. Preserve current runtime behaviour.
4. Do not wire runtime plugin execution into DB persistence yet.
5. Do not implement Home Assistant cache population, websocket integration, live state query, or device control.

Please inspect:
- docs/plugin-audit-db-api-design.md
- docs/plugin-audit-persistence.md
- docs/home-assistant-data-lifecycle.md
- resources/db/schema
- resources/db/schema/AGENT_CONTEXT.md
- any existing DB verification scripts/tests
- orac_core.plugin_invocations assets
- orac_core.plugin_audit_events assets
- orac_code plugin audit API/package assets, if present

Specific tasks:
1. Update documentation to reflect that plugin audit DB/API assets now exist.
2. Remove or reword stale statements saying object-by-object DDL/API work is still future work.
3. Update HA lifecycle docs so the next step is not "plugin audit DB/API design", but DB verification and runtime audit adapter work.
4. Add or extend DB verification checks for:
   - plugin_invocations table;
   - plugin_audit_events table;
   - primary keys;
   - foreign keys;
   - check constraints;
   - indexes;
   - triggers;
   - comments;
   - API views;
   - TAPI/API packages;
   - grants;
   - install-order inclusion.
5. Confirm the canonical abbreviation is:
   - plugin_invocations -> plg_inv
   - plugin_audit_events -> plg_audevt
6. Confirm no stale plg_aud_evt references remain.

Important constraints:
- Do not change plugin execution behaviour.
- Do not wire runtime audit persistence yet.
- Do not let plugins write audit tables directly.
- Do not implement Home Assistant runtime behaviour.
- Do not remove HA scaffold protection.
- Do not change protocol, provider, voice, confirmation, timeout, or policy behaviour.
- Use lowercase Oracle SQL/PLSQL reserved words.
- Preserve existing tests.

Validation:
- poetry run python -m unittest discover -s tests
- Run any existing DB/static verification command if present.

Please report:
- files changed;
- stale documentation corrected;
- DB verification added;
- plugin audit objects verified;
- any missing DB/API assets found;
- validation results;
- deferred work.

---

Please fix the current full-suite failures in tests/test_orac_voice.py around _FakeVoiceWorker.enqueue_text(... tts_voice=...).

Context:
- The previous pass only changed plugin audit documentation/schema verification.
- The full suite now fails in tests/test_orac_voice.py because the fake voice worker does not match the current production enqueue_text call signature.
- Do not change plugin audit code.
- Do not change database/schema files.
- Do not change Home Assistant behaviour.
- Do not change provider/plugin policy/confirmation/timeout behaviour.
- Keep the fix narrow.

Tasks:
1. Inspect the production TTS/voice worker enqueue_text signature and all call sites.
2. Update _FakeVoiceWorker in tests/test_orac_voice.py to match the production contract.
3. Prefer an explicit tts_voice optional keyword parameter if that is now part of the contract.
4. Only use **kwargs if the production contract genuinely supports arbitrary keyword arguments.
5. Preserve the existing voice contract tests.
6. Add assertions for tts_voice only if the tests already have enough context to do so cleanly.
7. Run:
   - poetry run python -m unittest tests.test_orac_voice
   - poetry run python -m unittest discover -s tests

Please report:
- files changed;
- root cause;
- whether the production contract or only the test fake was stale;
- validation results.

---

So what now remains?

---

Please implement the next Orac architecture remediation pass: add runtime plugin audit persistence through a narrow PluginAuditAdapter and wire it through PluginExecutionService.

Current state:
- The full test suite is green again.
- Plugin audit DB/API assets now exist and have static verification.
- plugin_invocations and plugin_audit_events are the durable audit tables.
- orac_code.plugin_audit_api exists as the approved database API boundary.
- PluginExecutionService currently acts as a useful seam, but not yet a full plugin execution lifecycle boundary.
- PluginRouter already handles policy, scaffold denial, confirmation checks, timeout handling, failed/timed-out provenance, and no-import denied paths.
- Plugins must not write audit records directly.
- Home Assistant remains scaffold-only.
- HA tables are cache/state tables, not audit tables.

Goal:
Add a runtime-facing plugin audit adapter and wire plugin execution lifecycle audit recording through PluginExecutionService, without changing plugin execution behaviour.

Important architecture rule:
Plugin audit persistence must be owned by Orac runtime code, not by plugins. Plugins should not know about or write to plugin_invocations or plugin_audit_events.

Please inspect:
- src/model/plugin_execution_service.py
- src/model/plugin_router.py
- src/model/plugin_execution_policy.py
- src/model/plugin_confirmation_broker.py
- src/controller/orac.py
- docs/plugin-audit-persistence.md
- docs/plugin-audit-db-api-design.md
- tests/test_plugin_router.py
- tests/test_orac_plugin_integration.py
- tests/test_plugin_audit_schema.py
- the orac_code.plugin_audit_api package assets under resources/db/schema

Tasks:

1. Add a PluginAuditAdapter
   - Suggested location: src/model/plugin_audit_adapter.py
   - The adapter should be the only Python-side seam for writing plugin audit records.
   - It should call the approved orac_code.plugin_audit_api path if runtime DB access is available.
   - If DB/context access is unavailable, it should degrade safely and log clearly, without breaking normal plugin execution.

2. Define a small Python-side audit payload model
   - Use dataclasses or existing project style.
   - Include fields such as:
     - request_id
     - correlation_id
     - turn_id
     - conversation_id
     - message_id
     - user_id
     - plugin_id
     - plugin_name
     - action_type
     - capabilities
     - entitlements
     - policy_decision
     - confirmation_id
     - confirmation_status
     - execution_status
     - timeout_seconds
     - failure_type
     - failure_message
     - provenance_json / provenance dict

3. Wire PluginExecutionService to the adapter
   - PluginExecutionService should coordinate audit recording for plugin execution outcomes.
   - Do not put audit writes inside plugin code.
   - Do not make PluginRouter responsible for database persistence unless it already owns the relevant context cleanly.
   - The service should record or prepare audit events for:
     - candidate selected, if candidate information is available;
     - policy evaluated;
     - confirmation required;
     - execution started;
     - execution completed;
     - execution failed;
     - execution timed out;
     - denied/scaffold/unknown-action outcomes.

4. Preserve existing behaviour
   - Weather plugin behaviour must remain unchanged.
   - Non-plugin prompts must still fall through to the LLM path.
   - Denied, scaffold, unknown-action, and confirmation-required paths must still avoid plugin import/execution.
   - Trusted confirmation behaviour must remain unchanged.
   - Timeout/failure provenance must remain unchanged from the caller’s perspective.
   - Existing response metadata and assistant-turn metadata must remain compatible.

5. Safe failure behaviour
   - Audit persistence failure must not cause normal safe plugin execution to fail unless the existing project explicitly treats audit persistence as mandatory.
   - Log audit persistence failures with useful developer detail.
   - Do not expose raw DB errors, stack traces, secrets, or internal SQL details to the user-facing response.

6. Tests
   Add or update tests proving:
   - PluginAuditAdapter can normalise completed plugin provenance into an audit payload.
   - Denied/scaffold/unknown-action/confirmation-required outcomes map to audit payloads.
   - Failed plugin provenance maps to audit payloads.
   - Timed-out plugin provenance maps to audit payloads.
   - PluginExecutionService calls the audit adapter for handled plugin outcomes.
   - PluginExecutionService still returns None for LLM fallback.
   - Audit adapter failure does not break existing plugin execution unless explicitly configured to do so.
   - Plugins do not write audit records directly.
   - Weather/read-only plugin behaviour remains unchanged.
   - Existing plugin provenance still reaches response metadata and assistant-turn metadata.

Important constraints:
- Do not implement real Home Assistant control.
- Do not populate HA tables.
- Do not implement HA websocket integration.
- Do not implement live HA state query.
- Do not remove HA scaffold protection.
- Do not change protocol frame names or payload structure.
- Do not change provider behaviour.
- Do not change voice behaviour.
- Do not change plugin policy, confirmation, timeout, or provenance semantics except to add audit persistence around them.
- Do not perform unrelated DB/APEX cleanup.
- Do not let plugins write audit tables directly.

Validation:
- poetry run python -m unittest tests.test_plugin_router
- poetry run python -m unittest tests.test_plugin_runtime
- poetry run python -m unittest tests.test_orac_plugin_integration
- poetry run python -m unittest tests.test_plugin_audit_schema
- poetry run python -m unittest tests.test_weather_plugin
- poetry run python -m unittest discover -s tests

Also run:
- poetry run python scripts/check_guardrails.py

Note:
scripts/check_guardrails.py may still fail if docs/agent-guardrails/Untitled Document remains present. Report that separately as a known guardrail hygiene issue, not as a plugin audit failure.

Please report:
- files changed;
- PluginAuditAdapter responsibilities;
- how audit payloads map to plugin_invocations and plugin_audit_events;
- where audit recording is wired into PluginExecutionService;
- which lifecycle events are recorded;
- behaviours preserved;
- validation results;
- any remaining audit gaps;
- deferred work.

---

Please commit changes

---

Any uncommitted changes?

---

Please implement the next Orac architecture remediation pass: clean the remaining guardrail hygiene issue and add a small audit-completeness improvement around plugin audit persistence.

Current state:
- The full unit test suite is green.
- PluginAuditAdapter exists.
- PluginExecutionService now wires runtime plugin audit persistence through PluginRouter/PluginAuditAdapter.
- Plugin audit DB/API assets exist and are statically verified.
- Plugin audit records are written through the approved orac_code.plugin_audit_api path.
- Plugins must not write audit records directly.
- Home Assistant remains scaffold-only.
- HA tables are cache/state tables, not audit tables.
- scripts/check_guardrails.py currently fails only because docs/agent-guardrails/Untitled Document exists.

Goals:
1. Remove or resolve the known guardrail hygiene issue:
   - docs/agent-guardrails/Untitled Document
2. Add a small audit-completeness improvement if there is a clean existing hook:
   - link the persisted assistant message_id back to the plugin audit invocation after assistant-turn persistence.
3. Do not perform broad architecture refactoring.
4. Do not implement Home Assistant cache population or control.
5. Preserve current plugin execution behaviour.

Tasks:

1. Guardrail cleanup
   - Inspect docs/agent-guardrails/Untitled Document.
   - If it is empty, stray, or clearly obsolete, remove it.
   - If it contains useful content, move/rename it into a valid guardrail document name following the NN-kebab-case.md convention.
   - After cleanup, scripts/check_guardrails.py should pass.

2. Plugin audit message_id linking
   - Inspect the current assistant-turn persistence flow.
   - Identify whether PluginAuditAdapter or PluginExecutionService has access to the plugin_invocation_id and the saved assistant message_id after _save_assistant_turn or equivalent persistence completes.
   - If a clean hook exists, add a small link/update call through the approved plugin audit API path.
   - Do not add direct table writes.
   - Do not let plugin code write audit data.
   - If no clean hook exists, do not force it. Document the missing seam and leave this deferred.

3. Audit completeness decision
   - Review whether declined can_handle paths and pre-execution load/instantiate failures can be audited cleanly with the current lifecycle context.
   - If they can be added without broad refactoring, add narrow audit events.
   - If they require a larger lifecycle redesign, document them as deferred.
   - Do not destabilise current no-import denial guarantees.

4. Optional config only if already consistent with project conventions
   - Inspect existing config style.
   - If straightforward, add a documented audit persistence mode:
     - best_effort
     - strict
   - Default must remain best_effort.
   - If adding this would be too invasive, document it as deferred instead.

Important constraints:
- Do not implement real Home Assistant control.
- Do not populate HA tables.
- Do not implement HA websocket integration.
- Do not implement live HA state query.
- Do not remove HA scaffold protection.
- Do not change protocol frame names or payload structure.
- Do not change provider behaviour.
- Do not change voice behaviour.
- Do not change plugin policy, confirmation, timeout, or provenance semantics.
- Do not perform unrelated DB/APEX cleanup.
- Do not add broad controller refactoring.
- Do not allow plugins to write audit tables directly.

Tests:
- Update or add tests proving:
  - scripts/check_guardrails.py passes after the stray file cleanup.
  - Existing plugin audit persistence behaviour still works.
  - If message_id linking is implemented, the audit adapter/API receives the saved message_id through the approved path.
  - If no clean message_id hook exists, no runtime behaviour changes are made.
  - Weather plugin behaviour remains unchanged.
  - Non-plugin prompts still fall through to the LLM path.
  - Denied/scaffold/unknown-action/confirmation-required paths still avoid plugin import/execution.

Validation:
- poetry run python scripts/check_guardrails.py
- poetry run python -m unittest tests.test_plugin_audit_adapter
- poetry run python -m unittest tests.test_plugin_router
- poetry run python -m unittest tests.test_orac_plugin_integration
- poetry run python -m unittest tests.test_plugin_audit_schema
- poetry run python -m unittest tests.test_weather_plugin
- poetry run python -m unittest discover -s tests

Please report:
- files changed;
- what happened to docs/agent-guardrails/Untitled Document;
- whether message_id audit linking was implemented or deferred;
- whether declined can_handle/load/instantiate failures were audited or deferred;
- whether audit persistence mode config was added or deferred;
- behaviours preserved;
- validation results;
- remaining audit gaps;
- recommended next step.