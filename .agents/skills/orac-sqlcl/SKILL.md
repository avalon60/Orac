---
name: orac-sqlcl
description: Use SQLcl MCP to inspect the Orac development database safely when checking schemas, objects, constraints, grants, package status, data shape, or database errors.
---

# Orac SQLcl MCP

Use the configured SQLcl MCP server when database inspection is needed for the Orac development database.

## Saved SQLcl Connection

Use the saved SQLcl connection:

```text
orac-db
```
